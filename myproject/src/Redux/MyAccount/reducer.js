const initialState = {
  users: [],
  user: {},
  loading: false,
};

const UserAccountReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default UserAccountReducer;
